//Valor por referência
//const nomes = ['eduardo', 'luiz', 'pedro']
// string, objetos , funcoes, numeros

/*const nomes = new Array('Eduardo', 'luiz', 'pedro', 'wallace', 'juca', 'camila')

const novo = nomes.slice(1,-2)

console.log(novo)*/

const nomes = ['luiz', 'otavio', 'miranda']
const nome = nomes.join(' ')

console.log(nome)
